# --- Sanity (from sanity.io/manage) ---
NEXT_PUBLIC_SANITY_PROJECT_ID=30cxkf1b
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2026-01-01
# Create at sanity.io/manage -> API -> Tokens (needs "Editor" perms for the sync route)
SANITY_API_WRITE_TOKEN=

# --- Upstash Redis (from console.upstash.com) ---
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# --- Security ---
# Random string you make up — protects /api/revalidate and /api/sync-linkedin from public calls
SYNC_SECRET=
